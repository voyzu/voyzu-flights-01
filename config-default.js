const defaultConfig = {
    // Production set up
    bypassAuth:false,
    hotReload: false,
    logAuth: false,
    logEvent: false,
    logPublicRoutes:false,
    logResponse: false,
    port: 2500,
    throwErrors: false
};

export default defaultConfig;